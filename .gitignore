/tmp
/out-tsc
/dist
/bridge-shim/build
.DS_Store
.backend.pid
backend.log

/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js

.vscode/*

# Vendored Forge engine (large binaries, fetched locally per-machine)
/vendor/

# User-saved decks (personal, no cloud sync — see deckbuilder patch)
/decks/

# Secrets — Clerk/Neon credentials (see .env.example, frontend/.env.example)
.env
.env.local
frontend/.env
frontend/.env.local