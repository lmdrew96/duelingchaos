# Copy to .env and fill in — only needed for saved decks (see src/db.ts,
# src/auth.ts). The game itself runs fine with neither of these set.

# Clerk dashboard → your app → API Keys → Secret key
CLERK_SECRET_KEY=

# Neon dashboard → your project → Connection string (pooled connection is fine)
DATABASE_URL=
