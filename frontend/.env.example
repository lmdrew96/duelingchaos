# Copy to .env and fill in — only needed for saved decks (see src/AuthHeader.tsx,
# src/Deckbuilder.tsx). The app runs fine without this: saved decks stay
# hidden and the deckbuilder/board work as normal.

# Clerk dashboard → your app → API Keys → Publishable key
VITE_CLERK_PUBLISHABLE_KEY=
